name: 2Dto3D-MCPServer
description: Convert base64 image of 2D maps into zipped 3D OBJ files using FastMCP
version: 1.0.0

startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      "command": "python",
      "args": ["mcp_server.py"]
    })